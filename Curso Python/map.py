
# Map Function
    # Muito utilizado com listas
    # Aplicar uma funcao a um interable, por item. (list, tuple, dic, etc.)

lista1 = [1, 2, 3, 4]

#def multi(x):         bora diminuir esse codigo
#    return x * 2

#lista2 = map(lambda x: x * 2, lista1)    ainda da para diminuir

print(list(map(lambda x: x * 2, lista1))) # ficou perfeito